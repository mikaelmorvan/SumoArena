package controller.signals
{
	import org.osflash.signals.Signal;
	
	public class ChangeTurnDurationSignal extends Signal
	{
		public function ChangeTurnDurationSignal()
		{
			super(uint);
		}
	}
}