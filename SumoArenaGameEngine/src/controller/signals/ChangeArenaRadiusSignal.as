package controller.signals
{
	import org.osflash.signals.Signal;
	
	public class ChangeArenaRadiusSignal extends Signal
	{
		public function ChangeArenaRadiusSignal()
		{
			super(uint);
		}
	}
}