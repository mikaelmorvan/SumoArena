package controller.signals
{
	import org.osflash.signals.Signal;
	
	public class ChangeShrinkingIntervalSignal extends Signal
	{
		public function ChangeShrinkingIntervalSignal()
		{
			super(uint);
		}
	}
}